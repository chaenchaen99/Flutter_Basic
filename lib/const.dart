void main() {
  //dart에서 const는 compile time constant를 만들어준다.
  const name = 'nico';
  //const는 compile할 떄 알고 있는 값에 사용하는 것이다.앱스토어에 올리기 전에 알고있는 값.
  //어떤 값인지 모르고 그 값이 API로부터 온다거나 사용자가 화면에서 입력해야하는 값이라면 그건 final이나 var이 되어야 한다.
  const max_allowed_price = 120;
  const PI = 3.141592;

}